@AGENTS.md

## Claude Code notes

- Before UI work, read the relevant guide: `docs/ui/photography.md` or `docs/ui/editions.md`.
- When implementing from Figma, follow the figma-design-to-code workflow: `get_design_context` first, adapt to the project's components and tokens, download assets into `src/assets/`.
- Keep Playwright scripts, screenshots and other scratch output in the session scratchpad, not the repo.
