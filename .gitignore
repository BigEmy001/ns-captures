node_modules
dist
.env
*.local
cloudinary-test.mjs
.DS_Store
scratch_upload.mjs
*.png
supabase/.temp
.vercel

# One-off debugging scripts. Two of these were committed to a public repo
# carrying the service_role key; scripts/no-secrets.mjs now blocks the value
# itself, and these keep the scratch files out in the first place.
check_*.mjs
check_*.cjs
fix_*.mjs
reset_*.cjs
update_*.js

# Photographer submissions dropped into the repo root while their work is
# catalogued. The photographs live in Cloudinary, not in git — Bogyeong Jin's
# folder alone is 96 MB. Put new ones in submissions/ and they are ignored
# automatically.
submissions/
Akiyama Tomoya from japan/
Bogyeong Jin from south korea/
Ryusei Yamada from japan\ /
